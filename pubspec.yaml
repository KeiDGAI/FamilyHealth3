name: family_health_app
version: 1.0.0+1
description: 家族健康アプリ

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  firebase_core: ^3.15.1
  firebase_auth: ^5.6.2
  cloud_firestore: ^5.6.11
  flutter_dotenv: ^5.0.2
  provider: ^6.0.5
  intl: ^0.18.1
  # Fitbit連携用
  http: ^1.1.0
  url_launcher: ^6.1.10
  # Huawei Health Kit連携用（Androidのみ）
  huawei_health: ^6.10.0+300

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
